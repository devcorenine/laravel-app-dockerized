name: Deploy Laravel with Docker

on:
  push:
    branches:
      - master

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build . -t ${{ secrets.DOCKER_USERNAME }}/my-laravel-app:${{ github.sha }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push Docker Image
        run: docker push {{ secrets.DOCKER_USERNAME }}/my-laravel-app:${{ github.sha }}

#      - name: Deploy to Production Server
#        uses: appleboy/ssh-action@master
#        with:
#          host: ${{ secrets.SERVER_IP }}
#          username: ${{ secrets.SSH_USER }}
#          key: ${{ secrets.SSH_KEY }}
#          script: |
#            cd /var/www/my-laravel-app
#            docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
#            docker pull my-laravel-app:${{ github.sha }}
#            docker-compose down
#            docker-compose up -d
#            docker-compose exec app php artisan migrate --force
#            docker-compose exec app php artisan cache:clear
#            docker image prune -f
